{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements_railway.txt"
  },
  "deploy": {
    "startCommand": "cd src && gunicorn --bind 0.0.0.0:$PORT main:create_app()",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
